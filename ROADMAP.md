# ROADMAP — MONEY

Живой план развития. **Что осталось** сделать. Готовые пункты удаляем отсюда и
переносим их описание в [README.md](README.md) (там — «как сервис работает сейчас»).
Исходное видение — в [SPEC.md](SPEC.md).

Нумерация (2.x / 3.x / 1.x) ссылается на обсуждение от 2026-06-18 (превращение в
дашборд аналитики). Порядок фаз — по зависимостям, не по вкусу.

Легенда: `[ ]` не начато · `[~]` в работе · `[x]` готово (перед удалением).

---

## Фаза A — Операции (фундамент данных) · read-only, без миграций
- [ ] **3.1** `GET /api/transactions` — фильтры (период/диапазон, тип, категория(и), счёт, поиск по продавцу/заметке, диапазон сумм), пагинация, **итог по выборке**.
- [ ] **2.2** Вкладка «Операции»: полный список, группировка по дням с дневным итогом, фильтры, быстрые чипы («этот/прошлый месяц», категория), итог выборки, тап → существующая шторка правки.
- [ ] **3.6** Индексы `transactions(type, datetime, category_id)` для быстрых выборок.

## Фаза B — Доходы по источникам (главный запрос)
- [ ] **INFRA** Alembic (или лёгкий миграционный раннер) — пререквизит: дальше меняем схему, нельзя терять живую БД.
- [ ] **3.2** `expected_monthly_income` = сумма активных `Recurring(type=income)` с конвертацией USD→RUB (вместо хардкода-настройки).
- [ ] **3.3** `Transaction.recurring_id` (nullable FK) + авто-матч факта к источнику (учится, как `CategoryRule`).
- [ ] **2.4.1–2.4.7** Вкладка «Доходы»: источники (CRUD: сумма, валюта, период, срок с/по, владелец я/жена, вкл/выкл); план дохода = сумма источников; план-факт по каждому; разбивка факта по источникам (donut+список); помесячная история; нудж «пора продлить» по `end_date`.

## Фаза C — Аналитика
- [ ] **3.4** `GET /api/analytics/*`: кэшфлоу-серия (доход/расход/нетто по месяцам), динамика категории, сравнение месяц-к-месяцу, топ-продавцы, доход-по-источникам, savings-rate.
- [ ] **2.3** Вкладка «Аналитика»: переключатель периода (месяц/квартал/год/произвольный), кэшфлоу-бары + линия нетто, сравнение М-к-М (Δ% зелёным/красным), donut структуры расходов + клик в динамику категории, топ-продавцы, норма сбережений, подписки (сумма+список из `detect_recurring`), (опц.) календарь-хитмап.

## Фаза D — Главная 2.0
- [ ] **2.1** Hero (капитал + Δ за месяц); три цифры месяца «Потрачено / Доход факт / Отложено» с Δ к прошлому; safe-to-spend (оставить); мини-кэшфлоу + спарклайн 6 мес; топ-категории с Δ; «N трат ждут разбора» (needs_review) кликабельно; «Все операции».

## Фаза E — Бюджет по категориям
- [ ] **3.5** (опц.) таблица `Budget` (категория, месяц, сумма) — иначе бюджет = прогноз (`category_forecast`).
- [ ] **2.4.8–2.4.9** Бюджет план-факт по категориям (прогресс-бары, перерасход красным); свод месяца: план дохода − план расходов − взносы в цели = ожидаемый профицит.

## Фаза F — Капитал 2.0
- [ ] **2.5** Линия net worth по месяцам (снимки уже копятся); Δ за месяц; разбивка прироста «от сбережений vs от курса».

## Фаза G — Алерты и нуджи
- [ ] **1.4** Событийные алерты в момент: перебор по категории, крупная трата, «N трат ждут разбора».
- [ ] **1.5 / 2.4.7** Напоминания продлить регулярное по `end_date` (пособия жены ~год).
- [ ] **4.2** Проактивные инсайт-карточки («подписки выросли на 20%», «кафе — антирекорд»).

## Фаза H — Данные-зависимое (когда накопится история)
- [ ] **1.1 / 2.3.8** Персональная инфляция — индекс корзины, подорожавшие товары (из позиций чеков).
- [ ] **1.2** Сверка счёта жены — ожидаемый остаток vs заявленный, поиск утечек (`ExternalReport`).

---

## Сквозное / инфра (по ходу)
- [ ] **1.6** Автодеплой GitHub→сервер — ждёт `gh auth refresh -h github.com -s workflow`.
- [ ] **1.7** Чистка эмодзи в ответах **бота** (мини-апп уже чистый).
- [ ] **1.9** Удалить демо-выписку (446 операций) перед стартом реального учёта — по сигналу пользователя.

## Доп. идеи (бэклог, не обязательны)
- [ ] **4.1** В боте вопрос-ответ: «сколько потратил на X в этом месяце».
- [ ] **4.3** Теги на транзакции (поверх категорий).
- [ ] **4.4** Сравнение год-к-году.
- [ ] **4.5** Экспорт CSV/Excel, PDF-отчёт за месяц.
