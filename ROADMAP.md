# ROADMAP — MONEY

Живой план развития. **Что осталось** сделать. Готовые пункты удаляем отсюда и
переносим их описание в [README.md](README.md) (там — «как сервис работает сейчас»).
Исходное видение — в [SPEC.md](SPEC.md).

Нумерация (2.x / 3.x / 1.x) ссылается на обсуждение от 2026-06-18 (превращение в
дашборд аналитики). Порядок фаз — по зависимостям, не по вкусу.

Легенда: `[ ]` не начато · `[~]` в работе · `[x]` готово (перед удалением).

---

## Фаза I — Умные инсайты · СЛЕДУЮЩАЯ ⭐
Расширяем «Финансовое здоровье» живыми наблюдениями на основе данных:
- [ ] **I.1** Аномалии трат по категориям (текущий месяц vs средние за 3 мес, +N%).
- [ ] **I.2** Spending velocity — на сегодняшний день месяца обгоняешь/отстаёшь от своего среднего темпа.
- [ ] **I.3** Топ-3 крупнейшие траты текущего месяца.
- [ ] **I.4** Subscription audit — давно не списывалось (>35 дней), подорожало (+20% vs медиана).
- [ ] **I.5** Pace к цели капитала и целям-копилкам.
- [ ] **I.6** Bills forecast — «обычно к этому числу приходит ЖКХ ~3500».

## Фаза J — Drilldown в данные
- [ ] **J.1** Клик по продавцу → все его операции + итоги за период (как клик по категории).
- [ ] **J.2** Поиск по сумме (фильтры `min_amount`/`max_amount` уже в API — добавить UI).
- [ ] **J.3** Bulk edit — выделить N операций → массовая правка категории.
- [ ] **J.4** Refunds tracking — отдельно показывать возвраты (чек n=2 уже отлавливается).
- [ ] **J.5** Year-over-year сравнение (когда накопится 12+ месяцев).
- [ ] **J.6** Просмотр состава прикреплённого чека в мини-аппе (`Receipt.fns_json` → позиции).

## Фаза K — Функциональные дыры
- [ ] **K.1** UI перевода между счетами (модель `type=transfer` уже есть).
- [ ] **K.2** Заметки на операции (поле `note` есть, UI нет).
- [ ] **K.3** Tax estimation — оценка налогов с USD-дохода (валютные правила РФ).
- [ ] **K.4** Goal templates — «отпуск», «машина», «образование», «свадьба».
- [ ] **K.5** Goal priorities + auto-funding в день дохода.

## Фаза L — UX-апгрейд
- [ ] **L.1** Telegram themeParams — автоматическая тёмная тема под Telegram.
- [ ] **L.2** Haptic feedback (`tg.HapticFeedback.impactOccurred`) на тапы кнопок.
- [ ] **L.3** Skeleton-loading вместо «Загрузка…».
- [ ] **L.4** Pull-to-refresh.
- [ ] **L.5** Empty states с подсказками («Долгов нет 👌 — добавить тут»).
- [ ] **L.6** Анимация перехода между вкладками.

## Фаза M — Качество и надёжность
- [ ] **M.1** Глубокий health check — пинг БД (сейчас только `{ok:true}`).
- [ ] **M.2** Backup retention — чистка старых архивов из чата бота (сейчас копятся).
- [ ] **M.3** Тесты на критичные пути: парсинг ФНС, склейка чек↔выписка, миграции, dashboard, capital, checkup.
- [ ] **M.4** Разнести `main.py` (866 строк) на роутеры (`routers/operations.py`, `capital.py` …).
- [ ] **M.5** Sentry или структурное логирование ошибок (опц.).

## Фаза H — Данные-зависимое (когда накопится история)
- [ ] **1.1 / 2.3.8** Персональная инфляция — индекс корзины, подорожавшие товары (из позиций чеков).
- [ ] **1.2** Сверка счёта жены — ожидаемый остаток vs заявленный, поиск утечек (`ExternalReport`).

---

## Сквозное / инфра (по ходу)
- [ ] **1.6** Автодеплой GitHub→сервер — ждёт `gh auth refresh -h github.com -s workflow`.
- [ ] **1.7** Чистка эмодзи в ответах **бота** (мини-апп уже чистый).
- ~~**1.9** Удалить демо-выписку~~ — отменено: 446 операций признаны реальными тратами за 3 мес, оставлены для статистики (2026-06-18).

## Доп. идеи (бэклог, не обязательны)
- [ ] **4.1** В боте вопрос-ответ: «сколько потратил на X в этом месяце».
- [ ] **4.3** Теги на транзакции (поверх категорий).
- [ ] **4.4** Сравнение год-к-году.
- [ ] **4.5** Экспорт CSV/Excel, PDF-отчёт за месяц.
- [ ] **4.6** Помесячная история дохода по источникам (стек-бар) — перенесено из Фазы C.
- [ ] **4.7** (опц.) В «Аналитике»: клик категории → её динамика по месяцам; календарь-хитмап трат.
- [ ] **4.8** Проактивные инсайт-карточки («подписки выросли на 20%», «кафе — антирекорд») — перенесено из Фазы G.

---

## Журнал
- **2026-06-18 · Долги: механика погашений** ✅ долг — отдельный реестр (НЕ доход/расход): `Debt.paid` (миграция), остаток = сумма−отдано, капитал учитывает остаток; `POST /api/debts/{id}/pay` (частичное погашение + авто-закрытие); в «Капитале» карточка долга с прогрессом «отдано X из Y · осталось Z», поле платежа, несколько контрагентов и оба направления.
- **2026-06-18 · Финансовый ассистент: полный набор** ✅ распределение капитала по валюте (RUB/USD, USDT=осознанный хедж) + подушка безопасности (мес расходов); вклады с доходностью (`Deposit` UI: ставка, капитализация, прогноз, начисленные %); финансовый чекап (`checkup.py`: норма сбережений, подушка, рекомендации) в «Аналитике»; net worth цель + прогноз достижения + «сначала заплати себе» (отложено/мес); цель↔счёт (`Goal.account_id`, сумма авто из баланса); «на разбор» (подсветка операций без категории) + кэш `no-store` для мини-аппа.
- **2026-06-18 · UI-апгрейд** ✅ шрифт Inter + плотнее размеры; новый кольцевой donut (круглые сегменты, %-пилюли, центр «всего N», без градиентов/стекла) в Аналитике/Доходах/Капитале; «Структура расходов» = donut + 2-колоночные карточки категорий (сумма/%/иконка) по референсу.
- **2026-06-18 · Приём чека ↔ выписка, токены, мелочи** ✅ двусторонняя склейка чек↔операция (±72ч, без дублей, обогащение составом); авто-обновление токена ФНС (на любую токен-ошибку + ежедневный прогрев 04:00, бот зовёт на вход если refresh умер); кнопка «📂 Открыть в приложении» под операцией (deep-link `?tx=`); фикс «Похоже на регулярные» (скрытие/добавление по обрезанному имени); клавиатура категорий в боте — частые вверх + «✕ Скрыть»; удалён дубль-категория «Сад+» (слита в «Сад»).
- **2026-06-18 · CRUD + шлифовка UI** ✅ ручное добавление операции (`POST /api/tx`) + форма в Операциях; CRUD счетов (создать/править/удалить-архив); правка суммы регуляров (`POST /api/recurring/{id}`); пастельный пикер категорий/источников (грид 2 кол., без обводки); группировка по дням в «Последних операциях» Главной (общий `groupedTx`); убран «запасной доход»; поля долгов выровнены (имя шире). Навбар 6 вкладок проверен в превью (375px).
- **2026-06-18 · Фаза A — Операции** ✅ `GET /api/transactions` (фильтры + итог по выборке) + вкладка «Операции» в мини-аппе + навбар на иконках. Описание перенесено в [README.md](README.md).
- **2026-06-18 · Фаза B — Доходы по источникам** ✅ источники (`Recurring type=income`, owner я/жена), миграции (`owner`, `recurring_id`, индексы), ожидаемый доход = сумма источников → safe-to-spend, авто-матч факта, вкладка «Доходы» (план-факт, donut «откуда пришло», нуджи продления). _Перенесено в Фазу C:_ помесячная история дохода по источникам.
- **2026-06-18 · Доходы: категории + привязка** ✅ доходные категории (Премия/Пособие/Прочий доход + пикер по типу `?type=income`), ручная привязка операции к источнику с обучением (`income_aliases`, `POST /api/tx/{id}/source`).
- **2026-06-18 · Фаза C — Аналитика** ✅ `GET /api/analytics?period=` + вкладка «Аналитика»: кэшфлоу доход/расход по месяцам, сравнение период-к-периоду по категориям (Δ%), структура расходов, топ-продавцы, норма сбережений, подписки. _В бэклог (4.6/4.7):_ помесячная история дохода, динамика категории по клику, хитмап.
- **2026-06-18 · Фаза D — Главная 2.0** ✅ четыре цифры месяца (Потрачено/Доход/Отложено/Капитал) со стрелками Δ к прошлому месяцу, карточка «N трат ждут разбора» → Операции, Δ у категорий, кнопка «Все операции». Доп. правки UI: нижнее меню — подпись под иконкой (мелко/нежирно); цифры над столбцами графика кэшфлоу. _(Мини-кэшфлоу на Главной не дублировал — он в «Аналитике».)_
- **2026-06-18 · Фаза E — Бюджет по категориям** ✅ `Budget` (лимит/категория) + `services/budget.py` + `GET/POST /api/budgets`. Секция «Бюджет · месяц» в «Аналитике»: лимит свой или авто-прогноз, план-факт с барами (перерасход красным), свод доход−расходы−цели = профицит; правка лимита в шторке.
- **2026-06-18 · Фаза F — Капитал 2.0** ✅ `services/capital.py` + `GET /api/capital`: линия net worth по снимкам, Δ за период, разбивка прироста (сбережения / курс $ / прочее). В «Капитале» — Δ в hero, график динамики, карточка разбивки. _(Линия/Δ наполнятся за ≥2 дня снимков; сейчас снимок один.)_
- **2026-06-18 · Клик по категории + Фаза G** ✅ клик категории на Главной → «Операции» за месяц по ней (`id` в `by_category`); `services/alerts.py`: алерты в момент траты (крупная ≥ `alert_big`, перебор бюджета) через `bot._reply`; нудж продления регулярных (`nudge_job`, 10:30). _В бэклог (4.8):_ проактивные инсайт-карточки.
