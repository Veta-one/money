# Замени YOUR_DOMAIN на свой домен.
# После получения сертификата (certbot) раскомментируй блок 443 и редирект.

server {
    listen 80;
    server_name YOUR_DOMAIN;
    client_max_body_size 15m;             # под фото чеков

    # статика мини-аппа (React build)
    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }

    # API мини-аппа
    location /api/ {
        proxy_pass http://app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Telegram webhook
    location = /webhook {
        proxy_pass http://app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# server {
#     listen 443 ssl http2;
#     server_name YOUR_DOMAIN;
#     ssl_certificate     /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#     add_header Strict-Transport-Security "max-age=31536000" always;
#     client_max_body_size 15m;
#     location / { root /usr/share/nginx/html; try_files $uri /index.html; }
#     location /api/    { proxy_pass http://app:8000; proxy_set_header Host $host; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; }
#     location = /webhook { proxy_pass http://app:8000; proxy_set_header Host $host; }
# }
